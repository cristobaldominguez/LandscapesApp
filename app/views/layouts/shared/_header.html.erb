<% image ||= '' %>

<div class="search__unclick"></div>
<div class="hero__div" <%= image.present? ? "style='background-image: url(#{image})'".html_safe : '' %>></div>
<header class="header__main">
  <div class="header__container">
	<%= yield %>
  </div>
</header>